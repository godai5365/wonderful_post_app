ja:
  activerecord:
    # ↓model名(つまりテーブル名)を宣言
    models:
      article: 記事

    # ↓modelのattribute名(つまりテーブル名のカラム名)を宣言
    attributes:
      article:
        title: タイトル
        content: 本文

  views:
    pagination:
      first: '最初'
      last: '最後'
      previous: '前'
      next: '次'
      truncate: '...'
